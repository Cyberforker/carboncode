# gcp skill inbox

这是从本机 Codex 技能目录提交到项目 inbox 的 `gcp` 技能，供其他环境拉取后安装/迁移使用。

源路径：`/Users/hecate/.codex/skills/gcp/SKILL.md`

建议安装到办公室环境：

```bash
mkdir -p ~/.codex/skills/gcp
cp .codex/inbox/gcp/SKILL.md ~/.codex/skills/gcp/SKILL.md
```

安装后可用 `$gcp` 或在对话中要求 `gcp` 来执行 Git commit & push 工作流。
